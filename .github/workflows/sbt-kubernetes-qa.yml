name: SBT Kubernetes QA

on:
  workflow_call:
    inputs:
      working-directory:
        description: Directory containing build.sbt.
        required: false
        type: string
        default: .
      java-version:
        description: Java version to install before running SBT.
        required: false
        type: string
        default: '8'
      runner:
        description: Runner label override for SBT checks. Leave empty to use CodeBuild.
        required: false
        type: string
        default: ''
      codebuild-project-name:
        description: CodeBuild project name used to build the runner label. Defaults to monarch-${repository-name}-build.
        required: false
        type: string
        default: ''
      aws-region:
        description: AWS region for CodeArtifact authentication.
        required: false
        type: string
        default: ap-southeast-2
      aws-role-to-assume:
        description: AWS role ARN used to access CodeArtifact.
        required: false
        type: string
        default: arn:aws:iam::948396734470:role/github-actions-codeartifact-read
      coverage-command:
        description: SBT coverage command.
        required: false
        type: string
        default: sbt -batch -Dsbt.supershell=false -Dsbt.color=false jacoco
      coverage-timeout-minutes:
        description: Maximum minutes to wait for the SBT coverage command.
        required: false
        type: number
        default: 20
      jacoco-report-directory:
        description: Directory containing the Jacoco report, relative to working-directory. Defaults to ${repository-name}-impl/target/scala-2.12/jacoco/report.
        required: false
        type: string
        default: ''
      jacoco-package-row-limit:
        description: Maximum package rows to include in the Jacoco PR comment.
        required: false
        type: number
        default: 20

permissions:
  contents: read
  id-token: write
  issues: write
  pull-requests: write

jobs:
  coverage:
    runs-on: ${{ inputs.runner != '' && inputs.runner || format('codebuild-{0}-{1}-{2}', inputs.codebuild-project-name != '' && inputs.codebuild-project-name || format('monarch-{0}-build', github.event.repository.name), github.run_id, github.run_attempt) }}
    permissions:
      contents: read
      id-token: write
      issues: write
      pull-requests: write
    defaults:
      run:
        working-directory: ${{ inputs.working-directory }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Checkout shared QA scripts
        uses: actions/checkout@v4
        with:
          repository: ${{ job.workflow_repository }}
          ref: ${{ job.workflow_sha }}
          path: .qv-shared-workflows
      - name: Prepare report metadata
        id: qa-meta
        run: |
          suffix='${{ inputs.working-directory }}'
          if [ "$suffix" = "." ]; then suffix="root"; fi
          suffix=$(printf '%s' "$suffix" | sed -E 's#[^A-Za-z0-9_.-]+#-#g')
          echo "artifact_suffix=$suffix" >> "$GITHUB_OUTPUT"
      - uses: actions/setup-java@v4
        with:
          distribution: corretto
          java-version: ${{ inputs.java-version }}
      - name: Cache SBT dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/coursier
            ~/.ivy2/cache
            ~/.sbt/boot
          key: ${{ runner.os }}-sbt-${{ github.event.repository.name }}-${{ hashFiles('**/build.sbt', '**/project/*.sbt', '**/project/build.properties') }}
          restore-keys: |
            ${{ runner.os }}-sbt-${{ github.event.repository.name }}-
            ${{ runner.os }}-sbt-
      - name: Clean restored SBT cache locks
        run: bash "$GITHUB_WORKSPACE/.qv-shared-workflows/.github/scripts/clean-sbt-cache-locks.sh"
      - uses: sbt/setup-sbt@v1
      - uses: aws-actions/configure-aws-credentials@v4
        if: ${{ inputs.aws-role-to-assume != '' || vars.AWS_CODEARTIFACT_READ_ROLE_ARN != '' }}
        with:
          role-to-assume: ${{ inputs.aws-role-to-assume || vars.AWS_CODEARTIFACT_READ_ROLE_ARN }}
          aws-region: ${{ inputs.aws-region }}
      - name: Export CodeArtifact token
        if: ${{ inputs.aws-role-to-assume != '' || vars.AWS_CODEARTIFACT_READ_ROLE_ARN != '' }}
        run: |
          CODEARTIFACT_AUTH_TOKEN=$(aws codeartifact get-authorization-token --domain quotable-value --domain-owner 948396734470 --region "${{ inputs.aws-region }}" --query authorizationToken --output text)
          echo "::add-mask::$CODEARTIFACT_AUTH_TOKEN"
          echo "CODEARTIFACT_AUTH_TOKEN=$CODEARTIFACT_AUTH_TOKEN" >> "$GITHUB_ENV"
          echo "CODEARTIFACT_TOKEN=$CODEARTIFACT_AUTH_TOKEN" >> "$GITHUB_ENV"
      - name: Fetch tags
        run: git fetch --tags --force
      - name: Run SBT coverage
        id: coverage
        env:
          COVERAGE_COMMAND: ${{ inputs.coverage-command }}
          COVERAGE_TIMEOUT_MINUTES: ${{ inputs.coverage-timeout-minutes }}
          COURSIER_PROGRESS: "false"
          SBT_OPTS: "-Dsbt.log.noformat=true"
        run: bash "$GITHUB_WORKSPACE/.qv-shared-workflows/.github/scripts/run-sbt-coverage.sh"
      - name: Clean SBT cache locks
        if: ${{ always() }}
        run: bash "$GITHUB_WORKSPACE/.qv-shared-workflows/.github/scripts/clean-sbt-cache-locks.sh"
      - name: Write Jacoco summary
        id: jacoco-summary
        if: ${{ always() }}
        env:
          COMMENT_FILE: ${{ runner.temp }}/sbt-jacoco-${{ steps.qa-meta.outputs.artifact_suffix }}.md
          JACOCO_PACKAGE_ROW_LIMIT: ${{ inputs.jacoco-package-row-limit }}
          JACOCO_REPORT_DIRECTORY: ${{ inputs.jacoco-report-directory != '' && inputs.jacoco-report-directory || format('{0}-impl/target/scala-2.12/jacoco/report', github.event.repository.name) }}
        run: bash "$GITHUB_WORKSPACE/.qv-shared-workflows/.github/scripts/write-sbt-jacoco-summary.sh"
      - uses: actions/github-script@v7
        if: ${{ always() && github.event_name == 'pull_request' && steps.jacoco-summary.outputs.has_structured_summary == 'true' }}
        env:
          COMMENT_FILE: ${{ steps.jacoco-summary.outputs.comment_file }}
          COMMENT_MARKER: '<!-- qv-qa-summary:sbt-jacoco:${{ steps.qa-meta.outputs.artifact_suffix }} -->'
        with:
          script: |
            const fs = require('fs');
            const marker = process.env.COMMENT_MARKER;
            const body = `${marker}\n${fs.readFileSync(process.env.COMMENT_FILE, 'utf8')}`;
            const { owner, repo } = context.repo;
            const issue_number = context.payload.pull_request.number;
            const { data: comments } = await github.rest.issues.listComments({ owner, repo, issue_number, per_page: 100 });
            const existing = comments.find((comment) => comment.user.type === 'Bot' && comment.body?.includes(marker));
            if (existing) {
              await github.rest.issues.updateComment({ owner, repo, comment_id: existing.id, body });
            } else {
              await github.rest.issues.createComment({ owner, repo, issue_number, body });
            }
